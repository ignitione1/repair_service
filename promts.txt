17.02.2026
15:40

use name: fullstackprofessional
use mcp: context7

Задача: на основе приложенного ТЗ составить подробный план разработки

Контекст:
– Проект: веб-сервис “Заявки в ремонтную службу”
– Проект должен запускаться локально через Docker Compose
– Backend + frontend в одном репозитории
– База данных: SQLite
– Упор на корректную бизнес-логику и безопасную работу с конкурентными запросами
– Это тестовое задание, важно показать инженерное мышление

Что нужно сделать:

1. Внимательно проанализируй ТЗ из файла.
2. Составь поэтапный план разработки, включая:
   – архитектуру приложения (frontend / backend / DB)
   – выбор стека (фреймворки, ORM, UI)
   – структуру проекта (папки, слои, сервисы)
3. Отдельно опиши:
   – модель данных (Request, User, роли, статусы)
   – механизм авторизации (упрощённый, с сидом пользователей)
   – бизнес-логику смены статусов заявок
4. Подробно распиши реализацию защиты от “гонки” для действия:
   “Взять в работу” (assigned → in_progress):
   – на уровне БД / транзакций / блокировок
   – какое поведение считается корректным
5. Опиши API (или server actions):
   – ключевые эндпоинты
   – входные данные
   – возможные ошибки
6. Распиши план автотестов:
   – какие сценарии тестировать
   – как тестировать race condition
7. Опиши, что должно быть в:
   – README.md
   – DECISIONS.md
8. Если уместно — предложи улучшения (audit log, UX, скрипты), но чётко отдели их от обязательной части.

Формат ответа:
– Чёткая структура
– Нумерованные шаги
– Конкретика
– Без написания кода, только план и архитектурные решения

Цель:
Получить план, по которому можно сразу начинать реализацию проекта.
_______________________________________________________________________________________________________________

17.02.2026
16:45

Задача: провести локальную pre-Docker проверку проекта

Что нужно сделать:

1. Проведи логическую ревизию проекта:
   – корректность бизнес-логики заявок
   – допустимые и недопустимые переходы статусов
   – разделение ролей: диспетчер / мастер

2. Проверь критичные места, которые часто ломаются в Docker:
   – относительные пути к БД (SQLite)
   – использование .env файлов
   – localhost / 127.0.0.1 / 0.0.0.0
   – зависимости, которые могут отсутствовать в контейнере
   – порты и binding

3. Проверь реализацию race condition для действия “Взять в работу”:
   – есть ли транзакция / атомарная операция
   – что произойдёт при двух параллельных запросах
   – какой ответ получает второй запрос
   – соответствует ли это ТЗ

4. Проверь тесты:
   – что они реально проверяют бизнес-логику
   – не зависят ли от локального окружения
   – можно ли их запускать автоматически перед сборкой Docker

5. Проверь сиды и миграции:
   – можно ли развернуть БД с нуля
   – есть ли диспетчер, мастера и заявки
   – не зависит ли сидинг от ручных действий

6. Проверь README.md:
   – можно ли по нему запустить проект с нуля
   – достаточно ли информации для проверяющего
   – есть ли инструкция по проверке “гонки” (curl / скрипт)

7. Составь чеклист “готово к Docker”:
   – что должно быть исправлено
   – что можно оставить как есть
   – что обязательно проверить ещё раз перед docker compose up

Формат ответа:
– Чеклист с пунктами (OK / ⚠ / ❌)
– Конкретные замечания и рекомендации
– Без написания Dockerfile или docker-compose
– Без написания кода, только анализ

Цель:
Найти и устранить все проблемы до упаковки проекта в Docker.
_______________________________________________________________________________________________________________

17.02.2026
17:00

Задача: улучшить визуальную часть проекта, не меняя бизнес-логику.

Проблема:
– Текущий дизайн выглядит устаревшим
– Плохая читаемость
– Нет визуальной иерархии
– Нет анимаций и ощущения “живого” интерфейса

Цель:
Сделать современный, удобный, полностью тёмный интерфейс,
который выглядит аккуратно и профессионально.

Ограничения:
– Не менять API и бизнес-логику
– Не ломать существующие сценарии
– Минимальные правки структуры компонентов
– Упор на UX, читаемость и визуальную ясность

Что нужно сделать:

1. Общий стиль
   – Полный dark theme (фон, карточки, формы, таблицы)
   – Хороший контраст текста и элементов
   – Акцентный цвет для действий (primary)
   – Вторичный цвет для статусов заявок

2. Читаемость
   – Увеличить line-height и отступы
   – Чёткая иерархия заголовков
   – Таблицы и списки заявок легко сканируются глазами
   – Статусы визуально различимы (цвет + бейдж)

3. Компоненты
   – Карточки заявок вместо “плоских” списков
   – Кнопки с hover / active состояниями
   – Формы с понятной валидацией и ошибками
   – Пустые состояния (нет заявок)

4. Анимации и микро-взаимодействия
   – Плавные переходы (fade / slide)
   – Анимация при:
     • создании заявки
     • смене статуса
     • назначении мастера
   – Лоадеры / skeleton при загрузке
   – Анимации не мешают, а помогают UX

5. Обратная связь пользователю
   – Toast / notification при успехе и ошибке
   – Ясные тексты сообщений
   – Подсветка изменённых элементов

6. Техническая реализация
   – Используй существующий стек проекта
   – Если есть CSS framework / UI library — придерживайся его
   – Если нет — предложи минималистичную систему стилей
   – Анимации должны быть лёгкими и производительными

Формат результата:
– Конкретные изменения компонентов и стилей
– Какие файлы править
– Какие анимации где добавить
– Без переписывания всего приложения с нуля

Критерий успеха:
Интерфейс выглядит современно, аккуратно и приятно.
_______________________________________________________________________________________________________________

17.02.2026
17:30

Задача: подготовить проект к сдаче 

Контекст:
– Проект полностью реализован (backend, frontend, UI)
– Бизнес-логика и дизайн завершены
– Проект должен запускаться локально через Docker Compose
– База данных: SQLite

Цель:
Сделать проект полностью готовым к проверке по ТЗ:
docker compose up → приложение работает → документация понятна.

Что нужно сделать:

1. Docker-упаковка
   – Подготовь Dockerfile (или Dockerfile’ы) для проекта
   – Подготовь docker-compose.yml
   – Корректно прокинь порты
   – Обеспечь сохранность SQLite (volume)

2. Переменные окружения
   – Выдели конфигурацию в .env (если уместно)
   – Подготовь .env.example
   – Убедись, что проект стартует без ручных правок

3. Миграции и сиды
   – Миграции применяются автоматически при старте
   – Сиды создают:
     • 1 диспетчера
     • 2 мастеров
     • несколько заявок
   – Проект поднимается “с нуля”

4. README.md (обязательно)
   Оформи README так, чтобы проверяющий мог:
   – понять, что это за проект
   – запустить его одной командой
   – войти под тестовыми пользователями
   – проверить race condition

   README должен содержать:
   – краткое описание проекта
   – требования
   – инструкция запуска
   – тестовые пользователи (логины/пароли)
   – описание API (кратко, если нужно)
   – как проверить “Взять в работу” в условиях гонки
     (curl / два терминала / скрипт)

5. DECISIONS.md
   Подготовь файл с 5–7 пунктами:
   – выбор стека
   – архитектура
   – подход к авторизации
   – реализация race condition
   – почему SQLite
   – ключевые компромиссы

6. Проверка соответствия ТЗ
   – Сверь каждый пункт ТЗ с реализацией
   – Укажи, где именно он выполнен

Формат результата:
– Готовые Docker-файлы
– Готовые README.md и DECISIONS.md
– Чёткая структура, без лишних пояснений
– Всё строго по ТЗ

Критерий успеха:
Проверяющий клонирует репозиторий,
выполняет docker compose up
и без вопросов понимает и проверяет проект.
_______________________________________________________________________________________________________________

17.02.2026
18:30

Задача: задеплоить готовый проект на VPS.

Контекст:
– Проект уже упакован в Docker и запускается через docker-compose
– VPS уже существует
– Доступ к серверу осуществляется по SSH
– SSH-ключ уже настроен и используется (создавать новый не нужно)
– На VPS уже есть другой проект, его нельзя затронуть или сломать

Цель:
Развернуть проект изолированно от других сервисов,
без конфликтов портов и конфигураций,
и сделать его доступным извне.

Ограничения:
– Использовать существующий SSH-ключ
– Не изменять конфигурацию чужого проекта
– Не останавливать существующие контейнеры
– Не использовать Kubernetes

Что нужно сделать:

1. Подключение к VPS
   – Используй существующий SSH-доступ

2. Изоляция проекта
   – Размести проект в отдельной директории
   – Используй отдельный docker-compose.yml
   – Используй уникальные:
     • container names
     • volumes
     • networks
   – Исключи конфликты с уже запущенными контейнерами

3. Проброс портов
   – Выбери свободный порт на VPS
   – Прокинь порт контейнера на внешний порт сервера
   – Проверь доступность сервиса извне
   – Убедись, что localhost/0.0.0.0 настроены корректно

4. Конфигурация окружения
   – Настрой .env для production
   – Убедись, что чувствительные данные не коммитятся
   – Проверь пути к SQLite и volume

5. Запуск и проверка
   – Запусти проект через docker compose
   – Проверь:
     • что контейнеры запущены
     • что приложение отвечает
     • что база данных работает
   – Проверь логи на ошибки

6. Минимальная защита
   – Убедись, что открыт только нужный порт
   – Проверь firewall 
   – Никаких лишних сервисов


Критерий успеха:
Проект работает на VPS,
доступен по IP:PORT,
не конфликтует с другими проектами
и может быть безопасно перезапущен.
